# Conversation history, oldest first

**[human] turn-1** — Two people could do this in parallel if the interface is fixed up front; that is why the
signatures in the task are exact. Do not change them.

**[human] turn-2** — Token TTL 15 minutes, single use, sha256 at rest. Cookie `session`, 24 hours.

**[human] turn-3** — We have six minutes of agent time for this. If the routes are done but the store is not (or
the other way round), the feature does not exist.
