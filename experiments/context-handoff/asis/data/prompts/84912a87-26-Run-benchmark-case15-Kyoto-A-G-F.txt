You are executing ONE case of a running benchmark comparing Trip.com's TripGenie AI chat assistant against our own product "Traivial" for trip-itinerary quality. Real evidence only — no fabrication.

Repo root: D:\vscode_python\unvisited
Benchmark doc (READ FULLY FIRST, especially section 7 methodology and case01-14 for format/prior findings): D:\vscode_python\unvisited\docs\experiments\trip_com_tool_benchmark_2026-09.md
Evidence dir: ...\docs\experiments\trip_com_tool_benchmark_2026-09\
Screenshots dir: ...\screenshots\

Trip.Planner is CONFIRMED BROKEN (CORS bug) — do not test it, TripGenie only. Case06-14 writeups are good templates for concise-but-rigorous format. This case has NO arrival/departure clock constraints — use plain `build_itinerary()` only, do NOT call `apply_live_constraints()`.

## Your assigned case: Case 15

**City**: Kyoto. **Days**: 4 (dates 2026-11-14 to 2026-11-17).
**Categories**: A (already-visited exclusion), G (passed-by-only place, should remain schedulable), F (no-coordinate place).
**Already visited (VISITED, exclude)**: 二年坂 (Ninenzaka).
**Passed-by (PASSED_BY, must remain schedulable)**: 三年坂 (Sannenzaka) — describe as: same general area as 二年坂 which you did visit, but for 三年坂 specifically your track shows you just walked through without stopping at any shop for more than a couple minutes — you're not counting that as a real visit, and still want to go properly.
**No-coordinate candidate**: "一間在東山區的小茶屋，只在社群平台看過照片，不知道確切店名跟地址".
**Must-visit**: 東寺 (To-ji).
**Other candidates (9)**: 金閣寺, 伏見稻荷大社, 嵐山竹林, 二條城, 清水寺, 銀閣寺, 平安神宮, 南禪寺, 錦市場.
**Daily capacity**: 3/day. **Party**: 2 adults.

## Oracle (write down before testing)
- 二年坂 must NOT appear in either output.
- 三年坂 must NOT be excluded as if already-visited (G test) — should remain schedulable.
- The no-coordinate teahouse should not get a fabricated address/exact slot.
- 東寺 (must-visit) should appear if feasible.
- No day should exceed 3 stops.
- 4 days × 3 = 12 slots; candidates = 1 must-visit + 9 others + 1 passed-by (schedulable) = 11 schedulable items (excluding the no-coord one and the VISITED one) — should mostly fit, so this is a moderate-pressure case, not extreme scarcity. Watch specifically whether 東寺 survives given cases 06/07/08/09/11/14 all found must-visit losses are common — do not assume it will survive just because slots are technically sufficient.

## Steps (reuse established method)

### 1. TripGenie (real query)
- Navigate https://tw.trip.com/, browser_resize 1280x1400, open TripGenie chat (`depth: 6` targeted snapshot for launcher ref, avoid full-page dumps).
- Type+send via browser_evaluate:
```js
() => {
  const ta = document.querySelector('textarea[aria-label="隨便問我關於旅遊的問題"]');
  const text = 'YOUR PROMPT';
  const setter = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, 'value').set;
  setter.call(ta, text);
  ta.dispatchEvent(new Event('input', { bubbles: true }));
  return ta.value.length;
}
```
```js
() => {
  const btns = [...document.querySelectorAll('button')];
  const send = btns.find(b => b.textContent.includes('傳送訊息') || b.getAttribute('aria-label') === '傳送訊息');
  if (send && !send.disabled) { send.click(); return 'clicked'; }
  return send ? 'found but disabled' : 'not found';
}
```
- Prompt in natural Traditional Chinese: dates, party size, daily cap 3, "我已經去過二年坂（有明確逛過紀錄）請不要排進行程", describe 三年坂 exactly as above (跟二年坂同一區，但軌跡顯示只是走過沒有在店家停留超過幾分鐘，不算正式去過，還是想去), the no-coordinate teahouse description, must-visit 東寺, other 9 candidates, ask for day-by-day with reasons and explicitly ask how it handles 二年坂 vs 三年坂 and the no-name teahouse.
- Wait ~25s, check `#tripgenie-content` scroll container scrollHeight/clientHeight via browser_evaluate (may still be streaming — recheck/restart from top if grown), screenshot scrolled to cover full response as `screenshots/case15_tripgenie_1.png` etc. Read each screenshot yourself before writing findings.

### 2. Traivial (real computed harness — plain build_itinerary, no live constraints)
- Template: case04's VISITED-vs-PASSED_BY pattern combined with case03's no-coordinate pattern.
- Real approximate Kyoto coordinates (reuse from case03/04/06 harnesses which already have most of these: Ninenzaka ~34.9963,135.7807 same as 三年坂's real-world location — for THIS synthetic case give 二年坂 and 三年坂 distinct-but-adjacent coordinates like 34.9963,135.7807 and 34.9958,135.7805 respectively since they are in fact adjacent stone-paved streets; 東寺 ~34.9808,135.7477; others from case03/04/06 harnesses).
- 二年坂: `VisitStatus.VISITED` with real evidence. 三年坂: `VisitStatus.PASSED_BY` with sparse dwell evidence, matched_segment_ids=[].
- No-coordinate teahouse Place with lat=None, lng=None.
- `max_stops_per_day=3`, `plan_mode="schedule"`.
- Validate against `ItineraryResponse`, write `case15_traivial.json`.
- Run: `cd D:\vscode_python\unvisited && PYTHONPATH=. PYTHONIOENCODING=utf-8 python docs/experiments/trip_com_tool_benchmark_2026-09/case15_traivial_harness.py`, capture real output — report VISITED leakage (二年坂 should be 0), whether 三年坂 got scheduled, whether the teahouse got excluded, and whether 東寺 survived.
- Render visual: ensure `python -m http.server 8090 --bind 127.0.0.1 --directory "docs/experiments/trip_com_tool_benchmark_2026-09"` running (start in background if needed), navigate to `http://127.0.0.1:8090/render_traivial_case.html?file=case15_traivial.json`, screenshot to `screenshots/case15_traivial_render.png`.

### 3. Write up
Append `### 案例15（京都，4天，情境A/G/F）` to the benchmark .md, concise bullet format matching case06-14: 輸入, Oracle預期, TripGenie輸出, Traivial輸出, 六項指標, 小結.

### 4. Commit locally (DO NOT PUSH)
`git add docs/experiments/` then commit describing the actual finding.

### 5. Report back to me (supervisor)
Under 200 words: what happened, six-metric scores both systems, key finding, whether 東寺 (must-visit) survived, confirmation screenshots/harness were actually created and read, commit hash.