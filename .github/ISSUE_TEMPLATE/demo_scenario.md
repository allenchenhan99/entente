---
name: Demo scenario
about: A mission + hand-written contracts that shows RelayGraph catching a real coordination failure
labels: scenario
---

**Mission title** (deliberately vague is fine):

**Target repo** (can be `demo-repo` or a script that materialises one):

**Contracts** (`examples/*.yaml`, validated by `LoadPlanBody`):

**What should happen**: which agent asks what, which criterion fails first, what the repair looks like.
